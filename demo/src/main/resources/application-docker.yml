spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/royaume
    username: royaume
    password: royaume
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
